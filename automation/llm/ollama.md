# Local LLM

The MVP compose stack does not require a local LLM. The search workflow returns mocked image results.

Later workflows can call a local Ollama or LM Studio endpoint for intent extraction.
